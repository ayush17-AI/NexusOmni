import MapViewer from '@/components/MapViewer';

export default function ErangelPage() {
    return (
        <MapViewer
            map="/maps/erangel.jpg"
            mapName="Erangel"
        />
    );
}

